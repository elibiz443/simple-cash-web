<main class="w-5/6 md:w-3/4 lg:w-1/2 xl:w-[40%] mx-auto p-6">
  <div class="mt-7 bg-white border border-gray-400 rounded-xl shadow-md shadow-slate-400">
    <div class="p-4 sm:p-7">
      <div class="text-center">
        <h1 class="block text-2xl font-bold text-gray-800">Sign in</h1>
        <p class="mt-2 text-sm text-gray-600">
          Don't have an account?
          <a class="text-blue-600 decoration-2 hover:underline font-medium" href="/signup">
            Sign up here
          </a>
        </p>
      </div>

      <div class="mt-5">
        <!-- Form -->
        <%= form_with(model: @user, url: signin_path, class: "flex flex-col space-y-2 lg:space-y-5") do |f| %>
          <div class="grid gap-y-4">
            <% flash.each do |type, msg| %>
              <div class="text-center w-[70%] mx-auto mt-2 mb-6">
                <%= content_tag :div, msg, class: build_alert_classes(type) %>
              </div>
            <% end %>

            <!-- Form Group -->
            <div>
              <div class="relative">
                <%= f.email_field :email, autofocus: false, autocomplete: "off", class: "py-3 px-4 block w-full border-2 border-gray-200 rounded-md text-sm outline-none focus:border-gray-500 focus:ring-gray-500", placeholder: "Email Address" %>
              </div>
            </div>
            <!-- End Form Group -->

            <!-- Form Group -->
            <div>
              <div class="relative">
                <%= f.password_field :password, id: "password", autocomplete: "new-password", class: "py-3 px-4 block w-full border-2 border-gray-200 rounded-md text-sm outline-none focus:border-gray-500 focus:ring-gray-500", placeholder: "Password" %>
              </div>
            </div>
            <!-- End Form Group -->

            <button type="submit" class="py-3 px-4 inline-flex justify-center items-center gap-2 rounded-md border border-transparent font-semibold bg-blue-500 text-white hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition-all text-sm mt-10">Sign In</button>
          </div>
        <% end %>
        <!-- End Form -->
      </div>
    </div>
  </div>
</main>
